name: vgrep
version: "2.6.1"
check: ${binary} --version
platforms:
  - linux/amd64
#- linux/arm64
tags:
  - category/operations
  - lang/go
  - type/cli
homepage: https://github.com/vrothberg/vgrep
description: User-friendly pager for grep
renovate:
  datasource: github-releases
  package: vrothberg/vgrep
  extractVersion: ^v(?<version>.+?)$
  priority: low
