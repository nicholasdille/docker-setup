name: tkn
version: 0.27.0
check: timeout 3 ${binary} version | cut -d' ' -f3
tags:
- type/cli
- type/development
- org/cdfoundation
- cicd
homepage: https://github.com/tektoncd/cli
description: CLI for interacting with Tekton
renovate:
  datasource: github-releases
  package: tektoncd/cli
  extractVersion: ^v(?<version>.+?)$