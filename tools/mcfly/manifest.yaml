name: mcfly
version: 0.6.1
check: ${binary} --version | cut -d' ' -f2
tags:
- type/cli
- terminal
homepage: https://github.com/cantino/mcfly
description: Fly through your shell history
renovate:
  datasource: github-releases
  package: cantino/mcfly
  extractVersion: ^v(?<version>.+?)$