name: pass-tail
version: "1.2.0"
check: ""
dependencies:
- pass
tags:
- category/security
- lang/shell
- type/cli
homepage: https://github.com/palortoff/pass-extension-tail
description: Pass extension to avoid printing the password to the console
renovate:
  datasource: github-releases
  package: palortoff/pass-extension-tail
  extractVersion: ^v(?<version>.+?)$