name: kluctl
version: "2.18.4"
check: ${binary} version 2>/dev/null
tags:
- type/cli
- category/development
- management
- lang/go
homepage: https://github.com/kluctl/kluctl
description: missing glue to put together large Kubernetes deployments, composed of multiple smaller parts in a manageable and unified way
renovate:
  datasource: github-releases
  package: kluctl/kluctl
  extractVersion: ^v(?<version>.+?)$