#syntax=docker/dockerfile:1.5.2

ARG ref=main

FROM ghcr.io/nicholasdille/docker-setup/nodejs:${ref} AS nodejs
FROM ghcr.io/nicholasdille/docker-setup/npm:${ref} AS npm

FROM ghcr.io/nicholasdille/docker-setup/base:${ref} AS prepare
ARG name
ARG version
COPY --link --from=nodejs / /
COPY --link --from=npm / /
WORKDIR ${prefix}${target}/libexec/nx
ARG name
ARG version
RUN <<EOF
npm install \
    --omit=dev \
    "nx@${version}"
ln --symbolic --relative --force "${prefix}${target}/libexec/nx/node_modules/.bin/nx" "${prefix}${target}/bin/"
EOF