name: shdotenv
version: 0.13.0
check: ${binary} --version
tags:
- type/cli
- category/development
homepage: https://github.com/ko1nksm/shdotenv
description: Support for POSIX-compliant and multiple .env file syntax
renovate:
  datasource: github-releases
  package: ko1nksm/shdotenv
  extractVersion: ^v(?<version>.+?)$