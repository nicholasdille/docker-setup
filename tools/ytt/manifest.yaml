name: ytt
version: "0.44.3"
check: ${binary} version | cut -d' ' -f3
platforms:
- linux/amd64
- linux/arm64
tags:
- org/vmware
- type/cli
- kubernetes
- templating
- package
- management
- lang/go
homepage: https://github.com/vmware-tanzu/carvel-ytt
description: YAML templating tool that works on YAML structure instead of text
renovate:
  datasource: github-releases
  package: vmware-tanzu/carvel-ytt
  extractVersion: ^v(?<version>.+?)$